document.addEventListener('DOMContentLoaded', () => {
    const dataContainer = document.getElementById('universities-data-container');
    dataContainer.innerHTML = '<p>We are currently working on integrating university data. Please check back later for updates.</p>';
});
